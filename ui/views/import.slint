export component ImportView inherits VerticalLayout {
    padding: 40px;
    alignment: center;
    spacing: 20px;

    Text {
        text: "📂 IMPORT DAT";
        color: #4aa3df;
        font-size: 32px;
        font-weight: 800;
        horizontal-alignment: center;
    }
    Text {
        text: "Zde bude probíhat zpracování DXF a PDF souborů.";
        color: #888;
        horizontal-alignment: center;
    }
}